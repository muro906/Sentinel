# Models placeholder — README
Place your exported production artifacts here after running Colab notebooks:

  encoder_weights.pt    — encoder-only state dict (from notebook 02)
  scaler.joblib         — fitted RobustScaler
  centroid.npy          — normal traffic centroid μ_norm
  model_meta.json       — threshold, embed_dim, feature_dim, dropout

These are generated by:
  hybrid-detection/notebooks/02_et_ssl_eval_export.ipynb → DRIVE/production/

Download from Google Drive and copy into this directory.
The detection-service container mounts this directory as /models (read-only).
